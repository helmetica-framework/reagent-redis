# Generated by `transmuter values --name instance --output test/unit/scenarios/ha/computed.yaml -f test/unit/scenarios/ha/values.yaml`. Do not edit.
#
# Regenerate with that command. Add --check to fail on a stale file instead of writing it.

redis:
  haproxy:
    enabled: true
  persistentVolume:
    size: 10Gi
  redis:
    config:
      maxmemory: 768mb
      min-replicas-to-write: 1
    resources:
      limits:
        cpu: 250m
        memory: 1Gi
      requests:
        cpu: 250m
        memory: 1Gi
  replicas: 3
  sentinel:
    quorum: 2
